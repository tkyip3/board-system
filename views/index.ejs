<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/bootstrap.min.css">
    <link rel='stylesheet' href='/css/style.css' />
    <title>留言版</title>
</head>
<body>
    <section>
        <div class="container">
            <div class="row">
                <div class="col">
                    <h1>留言版</h1>
                </div>
                <div class="col-auto">
                    <a href="/new-msg" class="btn btn-primary">新増留言</a>
                </div>
            </div>

            <div class="row row-cols-1 row-cols-md-3 g-4 mb-4">
                <% for( let i = 0; i < data.length; i++ ) { %>
                    <div class="col">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title"><strong><%= data[i].title %></strong></h5>
                                <p class="card-text"><%= data[i].detail %></p>
                                
                            </div>
                            <div class="card-footer">
                                <div class="row">
                                    <div class="col-6">
                                        <a href="/msg?_id=<%= data[i]._id %>" class="btn btn-sm btn-primary">查看留言</a>
                                    </div>
                                    <div class="col-6 text-end">
                                        <small class="text-muted"><%= data[i].date.getFullYear() %>-<%= data[i].date.getMonth() %>-<%= data[i].date.getDate() %> | <%= data[i].date.getHours() %>:<%= data[i].date.getMinutes() %></small>

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                <% } %>
            </div>

            <div class="text-center">
                <nav aria-label="Page navigation example">
                    <ul class="pagination justify-content-center">
                        <% if (pageNum != 1) { %>
                            <li class="page-item">
                                <a class="page-link" href="/?pageNum=<%= pageNum-1 %>" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                        <% } %>
                        <% for( let i = 0; i < pageTotel; i++ ) { %>
                            
                            <li class="page-item<% if (pageNum == i + 1) { %> active<% } %>"><a class="page-link" href="/?pageNum=<%= i+1 %>"><%= i+1 %></a></li>
                        <% } %>
                        <% if (pageNum != pageTotel) { %>

                            <li class="page-item">
                                <a class="page-link" href="/?pageNum=<%= pageNum+1 %>" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                            
                        <% } %>
                    </ul>
                </nav>
            </div>

        </div>
    </section>
    <% if(alert==="success"){ %>
        <script>
            alert("已提交留言");
        </script>
    <% } %>
    <% if(alert==="success"){ %>
        <script>
            alert("沒有這個留言");
        </script>
    <% } %>
</body>
</html>